## Chinese New Year Hidden-Object Web Game (3 Levels) with Internet Identity Auth and Persistent Progress

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
